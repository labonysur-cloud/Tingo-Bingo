rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // TingoBingo uses Cloudinary for storage, not Firebase Storage
    // Deny all access to prevent accidental usage
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
